name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: generate
      run:
       - mkdir _build
       - cmake -S . -B ./_build
    - name: build
      run: cmake --build ./_build
